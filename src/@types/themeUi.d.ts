declare module '@theme-ui/presets' {
  const object;
  export const base = object;
  export const bootstrap = object;
  export const bulma = object;
  export const dark = object;
  export const deep = object;
  export const funk = object;
  export const future = object;
  export const roboto = object;
  export const swiss = object;
  export const system = object;
  export const tailwind = object;
  export const tosh = object;
}
