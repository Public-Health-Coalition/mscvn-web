overwrite: true
schema: src/schema.graphql
documents:
  - 'src/documents/**/*.gql'
generates:
  src/generated/apollo.tsx:
    config:
      noNamespaces: true
      withComponent: true
      withHOC: true
      withHooks: true
    plugins:
      - add: |
          /* tslint:disable */
          /* eslint-disable */
      - typescript-client
      - typescript-common
      - typescript-react-apollo
